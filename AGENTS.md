# Agent Instructions

KRUG is a new local agent loops system.

Before non-trivial work:

1. Read `/home/vtima/project-contexts/context-devtools/project-map.yml`.
2. Read `/home/vtima/project-contexts/context-devtools/projects/krug.md`.
3. Search QMD with explicit collections only: `krug` and, when useful,
   `context-krug`.
4. Inspect current code before relying on external context.
5. Use Forgejo repository `vtima/context-krug` for active task tracking.

External context is advisory. Current product code, tests and durable in-repo
documentation are the source of truth.
